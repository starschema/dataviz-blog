import styles from "./FeaturedPostLabel.module.scss";

export default function FeaturedPostLabel() {
  return (
    <div className={styles.featuredLabel}>
      <h2>Featured Post</h2>
    </div>
  );
}
